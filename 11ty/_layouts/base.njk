<!DOCTYPE html>
<html>
    <head>
        <!-- Title and character set: -->
        <title>{{ title }}{% if page.url != '/' %} | {{ site.name }}{% endif %}</title>
        <meta charset="UTF-8" />
        <!-- Metadata: -->
        <meta name="description" content="{{ description or site.description }}" />
        <meta name="author" content="{{ author or site.author }}" />
        <!-- Viewport: -->
        <meta name="disabled-adaptations" content="watch" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
        <!-- Shortcut icons: -->
        <link rel="icon" href="{{ '/assets/images/logo/favicon.ico' | url }}" />
        <link rel="apple-touch-icon" href="{{ '/assets/images/logo/appicon.png' | url }}" />
        <!-- Bootstrap Icons: -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
        <!-- Stylesheet: -->
        <link rel="stylesheet" href="{{ '/assets/css/site.css' | url }}" />
        <!-- Headroom.js: -->
        <script src="{{ '/assets/js/headroom/headroom.min.js' | url }}"></script>
    </head>
    <body ontouchstart="">
        {{ content | safe }}
    </body>
</html>